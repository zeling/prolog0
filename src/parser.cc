//
// Created by 冯泽灵 on 2017/1/2.
//

#include "parser.h"

/*
void parser::parse_clause(parser_error *err) {
    token::token_type tok;
    tok = sc_.next();
    switch
}

void parser::parse_structure(parser_error *err) {
    token::token_type tok;
    tok = sc_.next();
    if (tok != token::token_type::ATOM) {

    }
}
 */
